# Fire-drill target for the distributed actionlint gate (t-ya55). The first
# revision of this file was DELIBERATELY broken (needs: named a job that does
# not exist) to prove the gate goes red; this revision is the fixed half that
# proves the gate goes green. The PR is closed without merging either way.
name: actionlint-fire-drill
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  drill:
    runs-on: ubuntu-latest
    steps:
      - run: echo "fire drill"
