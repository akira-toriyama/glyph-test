# furrow v6.0.0 live fire (rollout canary)

This file exists so the v6.0.0 task-status caller the canary apply delivered
runs on a real pull request in this repository — both halves: red on the
schema pre-flight while the shared board is v9, green on the same path once it
is v10. Delete it when the drill is over.
